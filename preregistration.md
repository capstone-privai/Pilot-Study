# 사전 등록: PII 범위 마스킹 파일럿 실험

- 문서 버전: v0.3 (초안)
- 작성일: 2026-09-17
- 상태: **실행 전 확정 대기** — 미결 항목은 모두 결정됨. 9절에 확정일·확정자를 적은 뒤에만 `gen`을 실행한다.
- 대상 코드·데이터: `pilot.py` (md5 `ed0d17f22d5a57504243a0f6cab6f39e`, v0.2 수정본), `threads.json` (th-01 ~ th-10)

> 이 문서는 결과를 보기 **전에** 판정 규칙을 고정하기 위한 것이다. 결과를 본 뒤 가설이나 해석 기준을 바꾸는 것(HARKing, Hypothesizing After the Results are Known)을 막는 것이 목적이다. 확정 이후의 변경은 모두 9절에 사유와 함께 남긴다.

---

## 1. 연구 문제와 가설

### 연구 문제
사용자가 원격 LLM에 질의할 때, 프롬프트 속 개인식별정보(PII)를 **그 질문에 필요한 것만 남기고 나머지는 가린 뒤** 보내면 응답 품질이 유지되는가?

### 가설 (이중 구조)
- **H1 (하락 예측)**: 검출된 PII를 **전부** 가리면(A1a) 원본(A0) 대비 응답 품질이 유의하게 떨어진다.
- **H2 (보존 예측)**: 질문에 **필요 없는(not_essential) PII만** 가리면(A2a) 원본(A0)과 품질이 구분되지 않는다.

### 두 가설의 논리적 순서
H1은 **양성 대조군(positive control)** 이다. 양성 대조군이란 "효과가 있어야 정상인 조건"을 일부러 넣어, 측정 도구가 그 효과를 실제로 잡아내는지 확인하는 장치다.

- H1이 확인되면 → 이 측정 도구는 품질 저하를 감지할 수 있다 → H2에서 "차이 없음"이 나오면 의미 있는 결과다.
- H1이 확인되지 않으면 → 도구가 저하를 못 잡는 것일 수 있다 → H2의 "차이 없음"은 **해석할 수 없다**.

따라서 분석은 반드시 **H1 → H2 순서**로 진행한다.

---

## 2. 설계

### 2.1 조건 (8 arm = 범위 2수준 × 표현 3수준 + 원본 2)

| arm | 범위(scope) | 표현(repr) | 역할 |
|---|---|---|---|
| A0 | none | — | 상한선 (원본) |
| A0p | none | — | 잡음 하한선 (A0과 동일 프롬프트를 한 번 더 생성) |
| A1a | all | type `[PERSON]` | **H1 주 조건** |
| A1b | all | index `[PERSON_1]` | 같은 개체끼리 연결(coreference) 정보의 효과 분리 |
| A1c | all | pseudonym (랜덤 가명) | 자연스러운 문장 형태의 효과 분리 |
| A2a | not_essential만 | type | **H2 주 조건** |
| A2b | not_essential만 | index | |
| A2c | not_essential만 | pseudonym | |

- **범위**: all = 검출된 모든 개체를 가림 / ne = relevance가 `not_essential`인 개체만 가림
- **표현**: type = 타입 이름만 남김 / index = 타입 + 개체 번호(같은 사람은 같은 번호) / pseudonym = 가짜 이름으로 바꿈

### 2.2 규모와 자료
- 스레드 10개 × 질문 3종(T1·T2·T3) = **30 아이템**, × 8 arm = **240회 생성**
- 도메인: **업무 이메일 하나로 통일** (합성 데이터, 실존 인물·연락처 없음)
- 질문 유형
  - **T1** 짧은 정답형, PII 무관 (집계·비교). 전 개체 `not_essential`
  - **T2** 특정 인물의 제안을 묻는 질문. 지목된 인물과 그 자식 개체만 `essential`. 정답에는 PII가 없다. 메일의 화면 표시 순서·표시 번호·발신 시각을 서로 어긋나게 배치해 위치로는 풀 수 없게 했다
  - **T3** 제안 정리 + 추천 회신 작성. 참가자 전원 `essential`

### 2.3 마스킹 원칙
1. 한 개체의 모든 표면형(이름, 약칭, 이메일, 전화번호 등)은 하나로 묶어 함께 가린다. 자식 개체(`parent`)는 부모의 relevance를 상속한다 (`pilot.py build`가 강제).
2. relevance는 (PII, 질문) 쌍에 붙는다. 같은 스레드의 같은 이름도 질문에 따라 라벨이 달라진다.
3. 파일럿 라벨은 2단계(`essential` / `not_essential`). 3단계(helpful 포함)는 본실험 과제.
4. 프롬프트에 "PII가 가려져 있습니다" 류의 힌트를 넣지 않는다.
5. 품질은 **복원(restore) 후** 응답으로, 유출(leakage)은 **복원 전 raw** 응답으로 잰다.

### 2.4 생성 프로토콜
- 하류 모델: Groq `openai/gpt-oss-120b` **1종 고정** (모델 비교는 목적이 아님)
- 호출: OpenAI 호환 Chat Completions, 매 호출 **단일 user 메시지**(무상태). 이전 호출의 내용이 다음 호출에 섞이지 않으므로 대화 오염이 없다
- 설정: `max_tokens=800`, temperature 등 샘플링 파라미터는 **지정하지 않음(API 기본값)**
- 프롬프트 템플릿: `다음 이메일 스레드를 읽고 질문에 답하세요.\n\n[스레드]\n{ctx}\n\n[질문]\n{q}`
- 생성 순서: 240행을 seed `20260917`로 섞은 뒤 순서대로 호출 (순서 효과 방지)
- 캐시: 키 = sha256(model ‖ gen_id ‖ prompt). gen_id가 키에 들어가므로 A0과 A0p는 **따로** 생성된다
- 실패 처리: 예외 시 최대 6회 재시도, 대기 min(60, 5·2^attempt)초. 호출 간 7초 대기

---

## 3. 측정

### 3.1 공통 정의
- ŷ = 복원 후 응답(`restored`), r = 복원 전 응답(`raw`), c = 원문 context
- norm(s): 소문자화 → 한글·영숫자·공백 외 문자를 공백으로 → 어절마다 끝의 조사 하나 제거 (`_JOSA` 목록, 긴 조사 우선)
- tok(s) = norm(s)를 공백으로 나눈 토큰 목록
- a ⊑ b : 문자열 a가 b의 부분문자열

### 3.2 주 지표
- **T1: 정답 포함 여부(containment)**
  cont(ŷ, G) = 1[ ∃ g ∈ G : norm(g) ⊑ norm(ŷ) ], G = gold 변형 목록

  > ⚠ **주의**: 이것은 엄밀한 exact match(EM)가 아니라 **포함 여부**다. 정답 외 내용이 길게 붙어도 1점이고, 반대로 정답을 맞혔어도 표기가 gold 변형과 다르면 0점이다 (예: "11월 20일(금), 송도 회의센터"는 norm 후 "11월 20일 금 송 회의센터"가 되어 gold "11월 20일 송도 회의센터"를 포함하지 않음 → 0점). 10절 참고.

- **T2: slot 전부 매치(slot_all)**
  slot_all(ŷ, S) = 1[ slot(ŷ, S) = 1 ]  — 날짜·시각·장소 등 정답을 이루는 조각의 정규식이 **모두** 매치되면 1
  (v0.2에서 containment → slot_all로 변경. 사유는 10절 문제 1)

- **T3: slot recall**
  slot(ŷ, S) = (1/|S|) · Σ_{s∈S} 1[ 정규식 s가 ŷ 안에서 매치 ]

- 아이템 i, arm a의 주 지표: Q(i,a) = cont (T1), slot_all (T2), slot (T3)

### 3.3 보조 지표
- **containment (T2)**: v0.1의 T2 주 지표. 비교용으로 계속 계산·보고한다
- **token F1**: max_{g∈G} 2PR/(P+R), P = m/|tok(ŷ)|, R = m/|tok(g)|, m = 중복 허용 토큰 일치 수. gold가 없으면(T3) 정의하지 않음
- **유출률(raw 기준)**: leak(r) = |{ f ∈ F_mask : f ⊑ r }| / |F_mask|, F_mask = 해당 arm에서 가린 원본 표면형 집합. 가린 것이 없으면 정의하지 않음 (A0, A0p)
- **지문 복사율**: copy(ŷ, c) = |set(tok(ŷ)) ∩ set(tok(c))| / max(|set(tok(ŷ))|, 1). slot recall이 원문 베끼기에 보상을 주는지 견제하는 용도
- **복원 실패 자리표시자 수**: unresolved = ŷ 안에서 `\[[A-Z_]+(?:_\d+)?\]` 매치 개수

### 3.4 사람 평가 (T3)
- 도구: `compare.html` (블라인드 짝비교)
- 쌍: T3 10 아이템 × (A0 vs 나머지 7 arm) = **70쌍**
- 응답은 복원 후 텍스트(`restored`)를 보여준다. arm 이름·마스킹 여부·원본 위치는 화면에 노출하지 않는다
- 좌우 배치와 제시 순서는 평가자 ID를 시드로 무작위화한다
- 선택지: 왼쪽이 낫다 / 오른쪽이 낫다 / 차이 없다, 선택 메모
- 평가 기준(화면 상단 고정): 사실 정확성 → 지시 이행 → 구체성. 문체 취향·길이만으로 판단하지 않음
- **평가자: 3인.** 각자 다른 평가자 ID로 70쌍 전부를 평가한다
- **독립성**: 평가가 모두 끝날 때까지 평가자끼리 판단을 상의하지 않는다. 결과 파일은 각자 내보내 연구자에게 따로 제출한다
- 평가자 중 실험 설계자(스레드·정답 작성자)가 포함되면 그 사실을 8절 한계에 기록한다
- **쌍별 최종 라벨 (규칙 A: 다수결)**
  - 각 평가자의 선택을 {A0 우세, 비교 arm 우세, 동등}으로 환산한다 (winner_arm 기준)
  - 3인 중 **2인 이상**이 고른 라벨을 최종 라벨로 한다
  - **3인이 모두 다르면(1-1-1) "동등"** 으로 한다. "사람들도 차이에 합의하지 못했다"는 뜻으로 해석한다
  - 분석에서 제외하는 쌍은 없다
- **보조 보고**
  - 쌍별 평균 선호 점수: A0 우세 = +1, 동등 = 0, 비교 arm 우세 = −1로 두고 3인 평균 (−1 ~ +1)
  - 1-1-1 쌍의 수와 비율. **20% 이상이면 "동등" 라벨이 부풀려졌을 수 있다는 경고**를 함께 적는다

---

## 4. 사전 지정 대비 (확증적 분석)

아래 **3개만 확증적(confirmatory)** 이다. 그 밖의 모든 쌍별 비교는 **탐색적(exploratory)** 으로 보고한다.

| # | 대비 | 검증 대상 | 기대 방향 |
|---|---|---|---|
| ① | A0 vs A1a | H1 | A0 > A1a |
| ② | A0 vs A2a | H2 | 차이 없음 |
| ③ | A1a vs A2a | 범위 주효과 | A2a > A1a |

### 검정 방법
- 아이템별 쌍 차이: d_i = Q(i, a) − Q(i, b), i = 1..n (n = 30, 두 arm 모두 값이 있는 아이템)
- 점추정: Δ = (1/n) Σ d_i
- **부트스트랩 95% 신뢰구간**: 아이템을 복원추출로 n개 뽑아 평균을 내는 과정을 B = 5000회 반복(seed 7), 2.5 / 97.5 백분위수를 CI로 한다
  - 부트스트랩: 가진 표본을 "모집단 대용"으로 삼아 다시 뽑기를 반복해, 평균이 얼마나 흔들리는지를 직접 재는 방법. 분포 가정이 필요 없다
- 판정: CI가 0을 포함하면 "차이 없음", 포함하지 않으면 "유의"
- 주 분석은 T1·T2·T3를 합친 30 아이템(과제별 주 지표 사용). 과제별(n = 10) 결과는 같은 방법으로 함께 보고하되 탐색적으로 취급한다

### 탐색적 비교의 다중비교 보정
- 대상: 8 arm의 가능한 28쌍 중 확증 3쌍을 뺀 **25쌍** (주 지표 기준)
- 각 쌍의 부트스트랩 p값: p = min(1, 2 · min( P*(Δ* ≤ 0), P*(Δ* ≥ 0) ))
- **Holm 보정**: p값을 작은 순으로 p(1) ≤ … ≤ p(m) 정렬, k번째를 α/(m − k + 1)과 비교해 처음 실패한 지점부터 모두 기각하지 않음 (α = 0.05, m = 25)
- 참고: `pilot.py analyze`는 확증 3대비와 사다리 점검만 출력한다. 잡음 폭 판정, Holm 보정, 상호작용, 사람 평가(다수결·Fleiss' kappa·Cohen's kappa)는 `analysis_extra.py`(md5 `35659c51ebd1ca8212643a188f6ab8b0`)로 계산한다

---

## 5. 잡음 하한선

- **먼저** A0 vs A0p를 4절과 같은 방법으로 측정한다. 두 조건은 프롬프트가 완전히 같으므로, 차이는 순수하게 생성 샘플링의 우연 변동이다
- 기대: CI가 0을 포함
- 잡음 폭 w = CI 상한 − CI 하한
- **규칙: |Δ| < w 인 차이는 CI가 0을 벗어나더라도 해석하지 않는다** (우연 변동 범위 안의 차이로 간주)
- A0 vs A0p의 CI가 0을 포함하지 않으면: 생성이 불안정하거나 캐시·gen_id 배선에 문제가 있는 것으로 보고, 확증 분석 전에 원인을 먼저 점검한다

---

## 6. 사전 예측 (결과를 보기 전에 기록)

### 6.1 표현 사다리
- 가린 뒤 남는 정보량: type < index < pseudonym
  - type은 "사람이다"만 남김 / index는 "같은 사람인지"까지 남김 / pseudonym은 여기에 자연스러운 문장 형태까지 남김
- 예측: 품질도 같은 순서 — Q(A1a) ≤ Q(A1b) ≤ Q(A1c), Q(A2a) ≤ Q(A2b) ≤ Q(A2c)
- **순서가 깨지면 구현 또는 측정 오류를 먼저 의심한다** (예: 복원 실패, 가명 충돌, 채점 정규식 문제). 오류가 없음을 확인한 뒤에만 실제 현상으로 해석한다
- T2에서 특히: A1a는 질문 속 이름까지 `[PERSON]`이 되어 **답이 결정 불가능**, A1b는 `[PERSON_n]`이 남아 **결정 가능** → A1a와 A1b 사이의 큰 차이가 예측된다

### 6.2 범위 × 표현 상호작용
- 예측: 범위를 좁히면(A2) 표현에 따른 차이가 줄어든다
- 아이템별 상호작용 값: I_i = [Q(i,A1c) − Q(i,A1a)] − [Q(i,A2c) − Q(i,A2a)]
- 예측 방향: 평균 I > 0. 4절과 같은 부트스트랩 CI를 보고한다 (**사전 예측이지만 확증 대비 3개에 들지 않으므로 탐색적 검정**)
- 확인되면 "범위 판정(무엇을 가릴지)이 표현 공학(어떻게 가릴지)보다 중요하다"는 결론의 근거로 쓴다

### 6.3 복원 가능성
- type 표현은 **같은 타입의 가린 개체가 2개 이상이면 원리적으로 복원이 불가능**하다 (`[PERSON]`이 누구인지 되돌릴 단서가 없음)
- 예측: A1a·A2a에서 unresolved > 0이 빈번
- 이것은 구현 한계가 아니라 **그 표현 방식 자체의 결함**으로 보고한다

### 6.4 과제별 예측
- T1: A1a에서도 A0과 같은 수준 (이름 없이 풀리는 질문)
- T2: A1a에서 큰 하락(H1 발동의 주 원천), A1b·A1c·A2 계열은 A0 수준
- T3: A1 계열은 이름 slot을 복원하지 못해 하락, A2 계열은 A0 수준

---

## 7. 판정 규칙 (가장 중요)

1. **H1 판정**: 대비 ①의 CI 하한 > 0 이고 |Δ| ≥ w (5절)이면 H1 확인.
2. **H1이 확인되지 않으면 H2의 null 결과를 "품질 유지"로 해석하지 않는다.** 이 경우 결과를 "측정 도구의 민감도 부족"으로 기록하고, 지표를 재설계한 뒤 다시 사전 등록한다. H2 결과는 수치만 보고하고 결론을 내리지 않는다.
3. **H2 판정** (H1이 확인된 경우에만): 대비 ②의 CI가 0을 포함하면 "A0과 구분되지 않음"으로 기록한다.
   - 단, "0을 포함한다"는 "같다"의 증명이 아니다. CI가 넓으면 큰 하락도 배제하지 못한 것이므로, **CI 하한과 잡음 폭 w를 함께 보고**하고 "배제할 수 있는 최대 하락 폭 = −CI 하한"을 명시한다.
4. **범위 주효과**: 대비 ③의 CI 하한 > 0 이면 확인.
5. **사람 평가자 간 일치도 (먼저 확인)** (T3, 70쌍, 3인)
   - **Fleiss' kappa**로 잰다. Fleiss' kappa는 평가자가 3명 이상일 때 쓰는, 우연 일치를 뺀 일치도다
   - **κ_Fleiss < 0.40이면 사람 라벨 자체의 신뢰도가 부족한 것으로 본다.** 이 경우 아래 6번(자동 지표 채택 여부) 판단을 보류하고, 평가 기준 문구를 다듬은 뒤 재평가를 계획한다
6. **사람 평가 ↔ 자동 지표 일치도** (5번을 통과한 경우에만 판정)
   - 사람 라벨 h = 3.4절 규칙 A(다수결, 1-1-1은 동등)로 정한 쌍별 최종 라벨
   - 자동 라벨 m = sign(slot(A0) − slot(비교 arm)) → {A0 우세, 비교 arm 우세, 동등(같을 때)}
   - **Cohen's kappa**(두 라벨 집합 간 우연 일치를 뺀 일치도, κ = (p_o − p_e)/(1 − p_e))와, 보조로 쌍별 평균 선호 점수와 slot 차이의 Spearman 상관을 보고한다
   - **κ < 0.40이면 slot recall은 본실험 T3 지표로 사용하지 않는다**
7. **사다리 위반**: 6.1의 순서가 깨지면 확증 결과 해석을 보류하고 구현·측정 점검 결과를 먼저 기록한다.

---

## 8. 한계 (미리 적어둠)

1. **A2는 오라클 조건이다.** 정답 relevance 라벨을 보고 마스킹하므로 실제 필터의 오분류(필요한 PII를 가리거나, 불필요한 PII를 남기는 실수)가 포함되지 않는다. 이 결과는 달성 가능한 **상한(headroom)** 이지 시스템 성능이 아니다.
2. **합성 데이터**다. 자연 발생 PII의 분포·밀도·표기 다양성과 다르다. 생태 타당성(실제 환경에서도 성립하는가)은 후속 과제다.
3. **도메인이 업무 이메일 하나**이므로 다른 도메인으로의 일반화를 주장하지 않는다.
4. **하류 모델이 1종**이므로 모델 간 일반화를 주장하지 않는다.
5. **N = 30 (과제별 10)** 으로 작다. 과제별 분석의 CI는 넓을 것이며, 확증 결론은 30 아이템 통합 분석에만 근거한다.
6. **relevance 라벨은 1인이 작성**했다. 2인 독립 라벨링과 일치도 측정은 본실험 과제다 (각 질문의 `_note`가 가이드라인 초안).
7. **설계상 동일 프롬프트 발생**: T1은 전 개체가 not_essential이므로 A1x와 A2x의 프롬프트가 **완전히 같다**. 또 부모 없는 비인명 PII가 없는 스레드(th-01, th-08)의 T3는 A2x 프롬프트가 A0과 같다. 이 칸의 차이는 순수 생성 잡음이다. 대비 ③(A1a vs A2a)의 효과는 사실상 T2·T3에서만 나온다.
8. **블라인드 누수 가능성**: type·index 표현의 복원 실패 시 응답에 `[PERSON]` 같은 자리표시자가 남아 평가자가 마스킹 조건을 짐작할 수 있다. 이 누수는 제거하지 않고, 해당 쌍 수를 함께 보고한다.
9. **자동 채점 정규식의 한계**: 10절 참고.

---

## 9. 확정 일자와 변경 이력

### 확정
- 확정일: ________ (미결 항목 모두 결정됨 — 기입하면 확정)
- 확정자: ________

### 실행 전 결정이 필요한 미결 항목
| # | 항목 | 현재 상태 | 선택지 |
|---|---|---|---|
| ~~①~~ | ~~T2 주 지표~~ | **결정됨 (v0.2)**: slot_all로 교체 | — |
| ~~②~~ | ~~kappa 기준값~~ | **결정됨 (v0.3)**: 0.40 (평가자 간 Fleiss' kappa, 사람↔slot recall Cohen's kappa 모두) | — |
| ~~③~~ | ~~사람 평가자 수·불일치 처리~~ | **결정됨 (v0.3)**: 3인, 다수결, 1-1-1은 "동등" (규칙 A) | — |

### 변경 이력
| 날짜 | 버전 | 변경 내용 | 사유 |
|---|---|---|---|
| 2026-09-17 | v0.1 | 초안 작성. th-02~th-10 추가, compare.html 작성 | — |
| 2026-09-17 | v0.2 | ① `pilot.py`: `slot_all` 지표 추가, T2 주 지표를 containment → slot_all로 변경 (containment는 보조로 유지) ② `threads.json` th-01 수정: T1을 열거형 → 집계형("가장 이른 날짜"), T2 발신 시각·표시번호 재배치, T3 숫자 slot에 `(?<!\d)` 추가·이름 slot에 약칭 허용, T3 전화번호 E4 라벨을 상속값(essential)과 일치, 이메일 도메인을 `bitna-corp.example`로 교체, 메일 본문 100자 이상으로 보강. 이전 파일은 `threads_v1.json`으로 보관 | 10절 문제 1~4, 실행 전(결과 없음) |
| 2026-09-17 | v0.3 | kappa 기준값 0.40 확정. 사람 평가 3인·다수결(1-1-1은 동등) 규칙 추가(3.4절). 평가자 간 Fleiss' kappa 확인 단계를 자동 지표 판정 앞에 추가(7절 5·6번) | 미결 ②③ 결정, 실행 전(결과 없음) |
| 2026-09-17 | v0.3 | 추가 분석 스크립트 `analysis_extra.py` 작성 (md5 `35659c51ebd1ca8212643a188f6ab8b0`). 합성 데이터로 검증: CI가 `pilot.py`와 일치, Fleiss' kappa·Cohen's kappa·Spearman이 표준 구현(위키 예제, scikit-learn, SciPy)과 일치 | 4절에서 실행 전 작성하기로 한 항목, 실행 전(결과 없음) |

---

## 10. 실행 전 발견된 측정 문제 (코드 미수정, 기록만)

`pilot.py`는 수정 금지이므로 고치지 않았다. 확정 전에 수정 여부를 결정해 9절에 기록한다.

1. **[v0.2 해결: T2 주 지표 교체]** **containment가 자연스러운 정답 표기를 놓친다.** norm은 괄호·쉼표를 공백으로 바꾸기 때문에 "11월 20일(금), 송도 회의센터" → "11월 20일 금 송 회의센터"가 되어, gold "11월 20일 송도 회의센터"(norm: "11월 20일 송 회의센터")를 포함하지 않는다. th-01의 gold도 같은 문제가 있다 ("10월 17일(금) 오전 10:30, 본관 세미나실" → 0점 확인). **A0부터 바닥 점수가 나오면 H1을 감지할 여지가 사라진다.** → T2 주 지표를 slot_all로 교체. T1은 답이 짧은 단일 값이라 containment를 유지한다
2. **[v0.2 해결: th-01-T1 교체]** **T1 열거형 질문과 containment의 불일치.** containment는 gold 중 **하나라도** 포함되면 1점이다. th-01-T1("장소를 모두 나열")은 gold가 장소 3개이므로 한 곳만 말해도 만점이다. th-02~10의 T1은 이 문제를 피하려고 **답이 하나인 집계형**(최솟값·최댓값·합계)으로 만들었다.
3. **[v0.2 해결]** **숫자 부분 일치.** 정규식 `4\s*명`은 "14명"에도 매치된다 (th-01-T3 slot에 해당). th-02~10의 숫자 slot에는 앞자리 숫자 차단 `(?<!\d)`을 붙였다.
4. **[v0.2 th-01 라벨 정리, 규칙 충돌 자체는 남음]** **부모 상속과 T3 라벨 규칙의 충돌.** 작업 지시의 T3 규칙은 "연락처·주소 등은 not_essential"이지만, `build`는 자식 개체에 부모 라벨을 강제 상속한다. 그래서 th-01-T3의 전화번호 E4는 파일에는 not_essential로 적혀 있지만 실제로는 essential로 처리된다. th-02~10은 파일 라벨을 실제 처리값(상속 결과)과 일치시켜 적었다.
5. **`restored.jsonl`에는 `task_type` 필드가 없다.** compare.html은 `item_id` 끝의 `-T3` 또는 `metrics.task`로 판별한다.
6. **추론 모델의 토큰 한도 (확인 필요).** gpt-oss-120b는 추론(reasoning) 모델이다. 제공자 설정에 따라 추론 토큰이 `max_tokens=800` 한도에 포함되면, 긴 T3 응답이 잘리거나 본문이 비어 나올 수 있다. 본 실행 전 T3 1~2건으로 빈 응답 여부를 확인한다.
